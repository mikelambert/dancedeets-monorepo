# Base dependencies for DanceDeets
# Python 3.11 compatible versions

# Image processing
Pillow>=10.0.0

# Language detection (cld2-cffi is deprecated, use langdetect or pycld3)
# cld2-cffi is not Python 3 compatible, consider alternatives:
# langdetect>=1.0.9
# or: pycld3>=0.22

# Geospatial
Shapely>=2.0.0

# Date/time parsing
dateparser>=1.1.0
timezonefinder>=6.0.0

# Memcache client
pylibmc>=1.6.0

# Google Cloud clients
google-api-python-client>=2.0.0
google-cloud-storage>=2.0.0
google-cloud-datastore>=2.0.0
google-cloud-ndb>=2.0.0
google-cloud-tasks>=2.0.0
