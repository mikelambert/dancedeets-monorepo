#extends _rsvp
#extends _results

#def main
	#if $past_results
		$render_results($past_results)
	#else
		#for $group in $grouped_upcoming_results
			#if $group.results
				$render_results($group.results)
				#break
			#end if
		#end for
	#end if
#end def
