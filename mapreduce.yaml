mapreduce:
- name: Compute City Rankings by Events
  mapper:
    input_reader: mapreduce.input_readers.DatastoreInputReader
    handler: logic.rankings.count_event_for_city
    params:
    - name: entity_kind
      default: events.eventdata.DBEvent
    - name: _app
      default: CITY_EVENT_RANKING
- name: Fix Bad Geocodes
  mapper:
    input_reader: mapreduce.input_readers.DatastoreInputReader
    handler: loc.gmaps_cached.cleanup_bad_data
    params:
    - name: entity_kind
      default: loc.gmaps_cached.CachedGeoCode
