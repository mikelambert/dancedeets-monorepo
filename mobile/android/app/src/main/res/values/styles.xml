<resources>

    <!-- Base application theme. -->
    <style name="StartupTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@drawable/scaled_launch_screen</item>
    </style>

</resources>
